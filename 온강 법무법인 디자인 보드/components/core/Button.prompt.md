온강 CTA 버튼. 상담 신청·더보기 등 행동 유도에 사용하며 항상 pill 형태입니다.

```jsx
<Button variant="primary">상담 신청</Button>
<Button variant="outline" withArrow={false}>더보기</Button>
```

변형: `variant` primary(레드)·dark(잉크)·light(화이트)·outline(테두리) / `size` sm·md·lg / `withArrow` 화살표 토글.
